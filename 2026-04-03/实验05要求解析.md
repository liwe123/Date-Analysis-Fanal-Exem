# 实验05要求解析（来自两份PDF）

## 一、核心技术要求（实验05：禁止循环）

1. 数据规模：使用约 1,000,000 行分钟级数据。
2. 关键限制：
   - 策略计算中禁止 `for` / `while` / `iterrows()`。
   - 需要使用 Pandas / NumPy 向量化方式实现。
3. 必做收益计算逻辑：
   - `pct_change` 计算市场分钟收益。
   - 信号要 `shift(1)` 避免未来函数。
   - `strategy_return = signal_shifted * pct_change`。
   - 累计收益使用 `(1 + strategy_return).cumprod()`。
4. 必需图表：
   - 同图对比 `Buy and Hold` vs `Your Strategy`。
5. 性能约束：
   - 代码需在 5 秒内运行（重点针对向量化策略块）。

## 二、提交物要求（更新后的AI集成作业框架）

你需要提交 3 部分：

1. 最终可运行代码（Notebook）
2. AI 对话记录（链接或PDF导出）
3. 高级工程师评审（1-2页，批判AI）
   - 你如何拆解问题、设计提示词
   - AI在哪些地方失败/低效/幻觉
   - 你如何迭代修正（改提示词还是手工改代码）

## 三、我已替你完成的部分

- 已解析两份PDF要求。
- 已在本地执行你提供的 Notebook 并成功跑通。
- 生成已执行版本：
  - `C:\Users\Administrator\.openclaw\workspace\485a1045-97a5-46a3-ae48-77cc4dd933ca.executed.ipynb`

## 四、你接下来直接可交付的建议

1. 使用已执行版 Notebook 作为主提交文件（或将其另存为最终命名）。
2. 再补一份“高级工程师评审”文档（1-2页）。
3. 附上本次 AI 对话记录（本会话可导出为PDF）。
